export interface Destination {
  id: number;
  country: string;
  title: string;
  subtitle: string;
  description: string;
  imageUrl: string;
  continent: string;
}

export interface NavItem {
  label: string;
  href: string;
}